{
  "name": "docker-express",
  "private": true,
  "version": "0.0.1",
  "description": "Node.js Hello world app using docker-compose",
  "dependencies": {
    "express": "4.13.3"
  },
  "scripts": {
    "start": "pm2 start processes.json --no-daemon"
  }
}
